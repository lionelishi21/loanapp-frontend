import { BaseModel } from '../../shared/models/base-model';

export class SetupModel extends BaseModel {
    requirements: any;
    all_set: any;
    already_installed: any;
    permissions: any;
    all_permission_set: any;
}
